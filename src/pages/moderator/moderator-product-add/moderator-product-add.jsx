import { ModeratorProductForm } from '../../../components/moderator-product-form/moderator-product-form';

export const ModeratorProductAdd = (props) => (
	<ModeratorProductForm {...props} isAdding />
);
